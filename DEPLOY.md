# Static deployment

This repository is ready for any static host. Serve the repository root; `index.html` contains the complete game.

Recommended public URL after GitHub Pages is enabled from `main` / root:

`https://mina-sayed.github.io/mina-dev-journey/`
